# Application-wide properties
app.name=TDD Frameworks
app.version=1.0.0

# Environment (dev, test, prod)
env=dev

# Base URL for the API
base.url=https://api.staging.com

# Enable debug logging
enable.debug.logging=true